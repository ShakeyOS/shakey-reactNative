import {StyleSheet, Text, View} from 'react-native';
import PageContainer from '../../components/PageContainer';
import Chat from '../../components/Chat/Chat';

const ChatScreen = () => {
  return (
        //Displaying the agent chat room
        <Chat />
  );
};

export default ChatScreen;

